# Summary

Predictive Coding is an alternative method to optimize deep nuearl networks in which updates are all local rather than global as is seen in the traditional back-propogation algorithm.
This repo gives a simple implementation of predictive coding in a sequential DNN with fully connected linear layers and ReLu activation and compares the optimization using predictive coding to optimization using standard back-propogation.
